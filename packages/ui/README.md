# UI Package

Shared UI primitives built with TailwindCSS/Shadcn patterns for AI Hotel Assistant V2.

## Scripts

- `npm run build` — bundle components with type declarations
- `npm run lint` — lint source files

## Usage

Import components from `@ai-hotel-assistant/ui`. Add new primitives in `src/` and export them through `src/index.ts`.
