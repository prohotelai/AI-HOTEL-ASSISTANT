# Config Package

Runtime configuration helpers for AI Hotel Assistant V2.

## Scripts

- `npm run build` — bundle configuration utilities
- `npm run lint` — lint source files

## Notes

Use `loadConfig` to centralize environment-driven settings and share them across apps.
