# Packages

Shared libraries for AI Hotel Assistant V2:

- `ui` — Shadcn/Tailwind UI primitives
- `config` — environment and runtime configuration helpers
- `utils` — reusable utility functions
- `types` — shared TypeScript definitions

Consume these packages from workspace apps via their scoped names.
