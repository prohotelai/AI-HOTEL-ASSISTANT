# Types Package

Shared TypeScript type definitions for AI Hotel Assistant V2.

## Scripts

- `npm run build` — emit bundled type declarations
- `npm run lint` — lint source files

## Notes

Add domain models in `src/` and export them through `src/index.ts` for reuse across apps.
