# Utils Package

Reusable utilities for AI Hotel Assistant V2.

## Scripts

- `npm run build` — bundle utilities
- `npm run lint` — lint source files

## Notes

Add shared helpers in `src/` and export them via `src/index.ts`. Includes a simple `cn` className joiner.
