# AI Engine

Node.js/Express backend scaffold for AI orchestration.

## Scripts

- `npm run dev` — start the development server with reload
- `npm run build` — compile TypeScript to `dist`
- `npm run start` — run compiled server
- `npm run lint` — lint source files

## Notes

Expose future AI pipeline endpoints from `src/`. Current `/health` route verifies service availability.
