# Widget SDK

Vanilla JS bundle scaffold for embedding the AI Hotel Assistant widget on partner websites.

## Scripts

- `npm run build` — bundle the SDK with TS declaration output
- `npm run lint` — lint source files

## Usage

Consume the built bundle from `dist/` or publish to a CDN. The `initHotelWidget` helper returns a mount API for rendering the widget placeholder.
