# Apps

Application surfaces for AI Hotel Assistant V2:

- `dashboard` — Next.js App Router dashboard
- `ai-engine` — Node.js/Express backend for AI orchestration
- `widget-sdk` — embeddable frontend bundle for partner sites

Each app has its own `Dockerfile` and package-level README.
