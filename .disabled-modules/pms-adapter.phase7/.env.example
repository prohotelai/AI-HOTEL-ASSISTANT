# ============================================================================
# PMS ADAPTER - ENVIRONMENT VARIABLES
# ============================================================================

# Global Feature Flag (OFF by default)
FEATURE_PMS_ADAPTER=false

# Encryption Key for PMS Credentials
# IMPORTANT: Generate a secure random string (32+ characters)
# Example: openssl rand -base64 32
PMS_ENCRYPTION_KEY=change-this-to-a-secure-random-key-minimum-32-chars

# ============================================================================
# USAGE INSTRUCTIONS
# ============================================================================
#
# 1. Copy this file to .env (if not already)
# 2. Generate a secure encryption key:
#    openssl rand -base64 32
# 3. Replace PMS_ENCRYPTION_KEY value
# 4. Keep FEATURE_PMS_ADAPTER=false until ready to use
# 5. Restart server after changes
#
# ============================================================================
# SECURITY NOTES
# ============================================================================
#
# - Never commit .env to version control
# - Use different keys for dev/staging/production
# - Rotate encryption key periodically
# - Backup key securely (loss = can't decrypt credentials)
#
# ============================================================================
